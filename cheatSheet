
# quotations, strings, escaping
'spam eggs'	# single quotes
'doesn\'t'	# use \' to escape the single quote...
"doesn't"	# ...or use double quotes instead

3*'un' + 'ium'	# n*str gives str repeated n times

'Py' 'thon'  	# str literals concatenated, useful for breaking long strings into multiple lines




str(smth)					- convert to string
x = int(input(“pease enter number”))		- invite to enter integer
---------------------------------------------------------------------------------------------------------
if 0==0:
	smth
elif 1==1:
	smth
else:
	smth
---------------------------------------------------------------------------------------------------------
for w in words:
	print(w, len(w))
---------------------------------------------------------------------------------------------------------
range(5)
range(1, 10)
range(1, 10, 3)
---------------------------------------------------------------------------------------------------------
for …:
else:
---------------------------------------------------------------------------------------------------------

